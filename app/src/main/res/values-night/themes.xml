<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PhoneNumberMaskApp" parent="Theme.Material3.DynamicColors.Dark.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/dark_green</item>
        <item name="colorOnPrimary">@color/light_green</item>
        <item name="android:textColor">@color/dark_green</item>
    </style>

    <style name="Theme.PhoneNumberMaskApp" parent="Base.Theme.PhoneNumberMaskApp" />
</resources>